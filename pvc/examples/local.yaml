apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-storage-pv-1
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
